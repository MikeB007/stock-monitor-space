-- MySQL Database Setup Script for Stock Monitor
-- Run this script as MySQL root user or with sufficient privileges

-- Create database
CREATE DATABASE IF NOT EXISTS mystocks;

-- Use the mystocks database
USE mystocks;

-- Show confirmation
SELECT 'Database mystocks created successfully for root user!' as Status;